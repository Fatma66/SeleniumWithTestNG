package Day3;

import com.sun.tools.javac.comp.Check;
import io.github.bonigarcia.wdm.WebDriverManager;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class do_yourself {
            /*
    navigate to herokuapp
    click AddRemoveElements
    Click Add button
    Check if Delete button is displayed using an if statement
 */


    public static void main(String[] args) {
      /*  WebDriverManager.chromedriver().setup();
        WebDriver driver = new ChromeDriver();
        driver.navigate().to("https://www.netflix.com");
        WebElement emailAdress =driver.findElement(By.xpath("//input[@type='id_email_hero_fuji']"));
        //emailAdress.click();
        emailAdress.sendKeys("admin");

        emailAdress.click();

    }

       */
    }
}